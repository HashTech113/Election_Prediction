# ============================================
# Backend Environment Variables (Example Template)
# Copy this to .env and fill in your actual values
# NEVER commit .env to Git!
# ============================================

# Server Configuration (Railway provides PORT automatically)
PORT=8001
HOST=0.0.0.0

# CORS allowlist (comma-separated). Use "*" for local dev only;
# in production set this to your Vercel domain, e.g.
#   https://your-app.vercel.app,https://www.your-app.com
CORS_ORIGINS=*

# Prediction source behavior
# 0 (default): require data/csv/predictions_2026.csv, fail if missing
# 1: allow fallback to data/csv/kerala_assembly_2026.csv
ALLOW_ASSEMBLY_FALLBACK=0

# Data Mode
# Set to 1/true/yes to use LIVE API calls for social media data
# Set to 0/false/no to use mock/cached data (recommended for testing)
USE_REAL_APIS=0

# Optional: Social Media API Keys (only needed if USE_REAL_APIS=1)

# X (Twitter) API v2 bearer token
# Get from: https://developer.twitter.com/en/portal/dashboard
X_BEARER_TOKEN=

# YouTube Data API key
# Get from: https://console.cloud.google.com/
YOUTUBE_API_KEY=

# NewsAPI key
# Get from: https://newsapi.org/
NEWS_API_KEY=
