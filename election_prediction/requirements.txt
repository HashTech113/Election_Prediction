# Unified Python dependencies for the Election Prediction Dashboard
# (covers both the Tamil Nadu and Kerala backends).
#
# Install with:
#     pip install -r requirements.txt

# --- Web framework / API ---------------------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# --- Data / ML stack -------------------------------------------------------
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
torch>=2.1
torchvision>=0.15.0
transformers>=4.30.0

# --- Utilities -------------------------------------------------------------
requests>=2.31
tqdm>=4.65.0

# --- Visualisation (used by Tamil Nadu training scripts) -------------------
matplotlib>=3.7
seaborn>=0.12
